apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myip-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: ip.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myip-service
                port:
                  number: 8080
